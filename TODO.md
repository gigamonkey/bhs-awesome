# TODO

# Quick

You can work on items in this section in the current branch.

- [ ] Please look at `format-ptx.py` which is a program to format documents in a
      particular XML. Using it as a model, can you write a more general purpose
      XML formatter, `format-xml.py` that does roughly the same things:
      indenting consistently to reflect the structure, wrapping the content of
      some elements in `CDATA` sections but only if needed based on their actual
      contents, and wrapping plain text elements. You may need to define some
      way to pass a configuration of which elements should be treated in which
      ways: i.e. which are block elements, which are inline, and which are code.

# Organizing

You can do work in this section in the current branch.



## Done

- [x] Please do what it says in [plans/extract-mcqs.md](plans/summarize-ced.md).

- [x] Please do what it says in [plans/summarize-ced.md](plans/summarize-ced.md).
